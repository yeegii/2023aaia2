///week14-3.cpp 課本page 2-09比大小的值
#include <stdio.h>
int main()
{
    int a = 5;
    ///if(a>0) ///if(判斷) 只做一次實驗 實驗完後註解掉

    ///{
        ///printf("a:%d\n", a);
    ///}
    ///while(a>0) ///while迴圈會一直做 實驗完後註解掉
    ///{
        ///printf("a:%d\n",a);
    ///}
    ///while(1) printf("a:1\n");///請問,這行執行時,會印東西嗎

    if(0) printf("請問0成立嗎?\n");///就只有0不成立
    if(1) printf("1成立\n");///其他全部都成
    if(2) printf("2成立\n");
    if(3) printf("3成立\n");
    if(-3) printf("-3成立\n");
}
